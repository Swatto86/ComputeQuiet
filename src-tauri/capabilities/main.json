{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least privilege for the main window. Every process, service, power and filesystem effect happens in Rust behind an explicit command, so the page needs no fs, shell or autostart permission: only the window controls it calls and event listening.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-theme",
    "core:window:allow-theme",
    "core:event:allow-listen"
  ]
}
