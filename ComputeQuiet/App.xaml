<Application x:Class="ComputeQuiet.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <SolidColorBrush x:Key="BgBrush" Color="#121416"/>
    <SolidColorBrush x:Key="BgWashBrush" Color="#1C2422"/>
    <SolidColorBrush x:Key="FgBrush" Color="#ECE8E0"/>
    <SolidColorBrush x:Key="MutedBrush" Color="#A09C94"/>
    <SolidColorBrush x:Key="DimBrush" Color="#8C8880"/>
    <SolidColorBrush x:Key="PanelBrush" Color="#1C1E20"/>
    <SolidColorBrush x:Key="QuietGreenBrush" Color="#3E8C6E"/>
    <SolidColorBrush x:Key="RestoreGoldBrush" Color="#D4A84B"/>
    <SolidColorBrush x:Key="RestoreFgBrush" Color="#181612"/>

    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
      <Setter Property="Margin" Value="0,0,0,10"/>
      <Setter Property="FontSize" Value="14"/>
    </Style>

    <Style x:Key="ToggleButtonStyle" TargetType="Button">
      <Setter Property="Height" Value="64"/>
      <Setter Property="MinWidth" Value="240"/>
      <Setter Property="FontSize" Value="18"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="24,12"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd"
                    Background="{TemplateBinding Background}"
                    CornerRadius="4"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Opacity" Value="0.92"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="bd" Property="Opacity" Value="0.55"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Application.Resources>
</Application>
