# Tests may unwrap: a panic there is a failing test, which is the point.
# Production paths propagate with `?` — the workspace lints enforce that.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
